# =============================================================================
# EFF Membership System - Production Environment Configuration
# =============================================================================
# Location: /var/www/eff-membership-system/.env
# This file should be placed in the REPOSITORY ROOT, not in backend/ folder
# =============================================================================

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=eff_admin
DB_PASSWORD=Frames!123
DB_NAME=eff_membership_database

# =============================================================================
# WebSocket Configuration
# =============================================================================
# For same-server deployment:
WEBSOCKET_URL=http://localhost:5000

# For domain/reverse proxy deployment:
# WEBSOCKET_URL=https://effmemberportal.org

# =============================================================================
# File Upload Configuration
# =============================================================================
# Upload directory (relative to repository root)
# Python processor watches this directory for new files
UPLOAD_DIR=_upload_file_directory

# =============================================================================
# Python Processor Configuration
# =============================================================================
# How often to check for new files (in seconds)
PROCESSING_INTERVAL=10

# Enable/disable IEC verification
IEC_VERIFICATION_ENABLED=true

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO
LOG_FILE=/var/www/eff-membership-system/backend/python/bulk_upload_processor.log

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
# 1. This .env file is used by the Python bulk upload processor
# 2. The backend has its own .env file in backend/.env
# 3. Both .env files should have UPLOAD_DIR set to the same value
# 4. The upload directory path should be relative to repository root
# 5. Ensure proper permissions:
#    - .env file: chmod 600 (read/write for owner only)
#    - Upload directory: chmod 755 (readable/writable by web server)
# 6. After creating this file, restart both backend and Python processor

