# SMS Provider Configuration
# Set SMS_PROVIDER to the provider you want to use

# Available providers: mock, json-applink, twilio, clickatell, gateway, smpp
SMS_PROVIDER=json-applink

# JSON Applink SMS Provider Configuration
# Replace these with your actual JSON Applink API credentials
JSON_APPLINK_API_URL=https://api.your-sms-provider.com/v1/send
JSON_APPLINK_API_KEY=your_api_key_here
JSON_APPLINK_USERNAME=your_username_here
JSON_APPLINK_PASSWORD=your_password_here
JSON_APPLINK_FROM_NUMBER=+27123456789
JSON_APPLINK_RATE_LIMIT=100

# SMS Webhook Configuration
# URL where delivery status callbacks will be sent
SMS_CALLBACK_URL=https://your-domain.com/api/v1/sms-webhooks/delivery/json-applink

# Alternative SMS Providers (for reference)

# Twilio Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+1234567890

# Clickatell Configuration
CLICKATELL_API_KEY=your_clickatell_api_key

# Generic SMS Gateway Configuration
SMS_GATEWAY_URL=https://api.your-gateway.com/sms/send
SMS_GATEWAY_API_KEY=your_gateway_api_key
SMS_GATEWAY_USERNAME=your_gateway_username
SMS_GATEWAY_PASSWORD=your_gateway_password

# SMPP Configuration
SMPP_HOST=smpp.your-provider.com
SMPP_PORT=2775
SMPP_SYSTEM_ID=your_system_id
SMPP_PASSWORD=your_smpp_password
SMPP_SYSTEM_TYPE=your_system_type
SMPP_ADDR_TON=0
SMPP_ADDR_NPI=0
SMPP_ADDRESS_RANGE=

# SMS Configuration Notes:
# 
# 1. JSON Applink Provider:
#    - This is a flexible provider that can adapt to most JSON-based SMS APIs
#    - Configure the API URL, authentication, and rate limits according to your provider's documentation
#    - The provider supports both API key and username/password authentication
#    - Rate limiting is enforced to prevent exceeding provider limits
#
# 2. Webhook Configuration:
#    - Set SMS_CALLBACK_URL to your public domain where delivery status updates will be received
#    - Make sure your firewall allows incoming connections to the webhook endpoint
#    - The webhook endpoint is: /api/v1/sms-webhooks/delivery/json-applink
#
# 3. Testing:
#    - Use the mock provider for development and testing
#    - Test your configuration using the SMS integration test endpoints
#    - Monitor provider health and delivery statistics through the admin interface
#
# 4. Production Deployment:
#    - Always use HTTPS for webhook URLs in production
#    - Monitor delivery rates and provider health regularly
#    - Set up appropriate rate limits based on your provider's requirements
#    - Keep API credentials secure and rotate them regularly
#
# 5. Failover Configuration:
#    - Consider setting up multiple providers for redundancy
#    - Monitor provider health and switch providers if needed
#    - Implement retry logic for failed messages
