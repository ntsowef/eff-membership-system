const { Pool } = require('pg'); const pool = new Pool({ host: '69.164.245.173', user: 'eff_admin', password: 'Frames!123', database: 'eff_membership_database', port: 5432 }); pool.query('SELECT table_name FROM information_schema.tables WHERE table_name = \\'sms_send_log\\'').then(result => { if (result.rows.length > 0) { console.log('SUCCESS: Table sms_send_log exists on production!'); } else { console.log('ERROR: Table not found'); } pool.end(); }).catch(err => { console.error('Error:', err.message); pool.end(); });
